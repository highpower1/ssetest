# Historical notes

Working documents kept from the port itself. They record what was measured on a
live runtime — render-target layouts, verified hook offsets, camera and depth
findings — and are preserved because that evidence is expensive to re-gather.

They are not maintained, and where they disagree with `docs/ARCHITECTURE.md`,
the architecture document is correct.
