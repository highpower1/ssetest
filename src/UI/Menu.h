#pragma once

namespace UI::Menu
{
	// Registers the "SkyrimUpscaler / Settings" page with SKSE Menu Framework 3
	// (QTR-Modding). No-op (with a log line) if the framework is not installed.
	void Register();
}
